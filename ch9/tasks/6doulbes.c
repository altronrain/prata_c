/* 6doubles.c -- переприсваивание значений по меньшего к большему

Напишите и протестируйте функцию, которая принимает в качестве аргументов
адреса трех переменных double и помещает наименьшее значение в первую
переменную, среднее значение -- во вторую, а наибольшее значение -- в третью.
*/
#include <stdio.h>

void ascend_sort (double *, double *, double *);

int main (void)
{
	double n1, n2, n3;

	printf ("Введите три числа с плавающей точкой через пробел:\n");

	while (scanf ("%lf %lf %lf", &n1, &n2, &n3) != 3) {
		int c;

		while ((c = getchar ()) != '\n' && c != EOF)
			;

		printf ("Введите требуемые данные, например: '3.27 6.77 2.44'\n");
	}

	ascend_sort (&n1, &n2, &n3);

	printf ("Введенные числа в порядке от меньшего к большему:\n");
	printf ("%.2lf %.2lf %.2lf\n", n1, n2, n3);

	return 0;
}

void swap (double *a, double *b)
{
	double temp = *a;
	*a = *b;
	*b = temp;
}

/* Спасибо Qwen за алгоритм, я туповат */
void ascend_sort (double *x, double *y, double *z)
{
	if (*x > *y)
		swap (x, y);

	if (*y > *z)
		swap (y, z);

	/* Самое большое точно в z, но нужно снова проверить x и y */
	if (*x > *y)
		swap (x, y);
}
/*
$ ./6doulbes 
Введите три числа с плавающей точкой через пробел:
7.88 4.23 5.62
Введенные числа в порядке от меньшего к большему:
4.23 5.62 7.88
*/