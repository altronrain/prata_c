#include <stdio.h>

int main(void)
{
    int toes = 10;

    printf("Заданное значение переменной: %d\n", toes);
    printf("Удвоенное значение этой переменной: %d\n", toes * 2);
    printf("Квадрат этой переменной: %d\n", toes * toes);

    return 0;
}